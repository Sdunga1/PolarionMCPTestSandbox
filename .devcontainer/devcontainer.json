{
  "name": "mcp-copilot-sandbox",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {},
  "forwardPorts": [3000],
  "remoteEnv": {
    "MCP_SERVER_URL": "http://localhost:3000"
  },
  "postCreateCommand": "bash -lc 'curl -Ls https://astral.sh/uv/install.sh | sh && npm install -g @modelcontextprotocol/cli && mkdir -p .vscode && cp .devcontainer/settings.json .vscode/settings.json'",
  "customizations": {
    "vscode": {
      "extensions": ["GitHub.copilot", "github.copilot-chat"],
      "settings": {
        "mcp.configPath": "${workspaceFolder}/mcp.json"
      }
    }
  }
}
